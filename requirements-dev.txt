# Kun til prøverne. Hører ikke med i det, der ruller ud — serveren skal
# ikke bære en testramme.
#
#   pip install -r requirements.txt -r requirements-dev.txt
#   python -m pytest
pytest==9.0.3
pytest-asyncio==1.4.0
